# clasp OAuth token — never commit
.clasprc.json

# Local env / secrets
.env
.env.*
*.pem
credentials.json
secrets.json

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo

# Node (if added later)
node_modules/
